- 数组缓冲区

  - unsigned long copy_to_user(void \_\_user *to, const void *from, unsigned long n);

    - 从内核 buf 写入用户 buf，返回“拷贝失败的字节数”

  * unsigned long copy_from_user(void *to, const void \_\_user *from, unsigned long n);

    - 从用户 buf 拷贝到内核 buf，返回“拷贝失败的字节数”

- kfifo 环形缓冲区

  - DEFINE_KFIFO(fifo_name, type, size);

    - 创建 struct kfifo 环形缓冲区

  - unsigned int kfifo_from_user(struct kfifo *fifo, const void \_\_user *from, unsigned int len, unsigned int \*copied);

  - unsigned int kfifo_to_user(struct kfifo *fifo, void \_\_user *to, unsigned int len, unsigned int \*copied);
